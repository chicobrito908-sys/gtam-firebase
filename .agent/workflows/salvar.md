---
description: Como realizar a blindagem (testes), gerar log e realizar o push para o GitHub
---
1. Execute o agente de testes para verificar se não há erros críticos no código.
// turbo
2. Gerar o log de implementação detalhado no arquivo `docs/logs/log_sessao.md`.
// turbo
3. Executar o comando: `git add .`
// turbo
4. Executar o comando: `git commit -m "feat: atualização de infraestrutura e regras SDD"`
// turbo
5. Executar o comando: `git push origin main`
